using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace JSI
{
    public class JSIEngine:IJSIModule
    {
        //else if (pm is MultiModeEngine)
        //{
        //    var thatMME = pm as MultiModeEngine;
        //    JUtil.LogMessage(this, "Set running primary? Is {0}, should be {1}; mode {2}", thatMME.runningPrimary, !vessel.ActionGroups.groups[actionGroupID[0]], thatMME.mode);
        //    var events = thatMME.Events;
        //    foreach (var ev in events)
        //    {
        //        JUtil.LogMessage(this, "... event {0} / {1}", ev.name, ev.guiName);
        //        if(ev.name == "ModeEvent")
        //        {
        //            if(thatMME.runningPrimary != vessel.ActionGroups.groups[actionGroupID[0]])
        //            {
        //                ev.Invoke();
        //            }
        //        }
        //    }
        //}
    }
}
